import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d32f2f', 
    },
    background: {
        default: '#f5f5f5',
      },
  },
  
});

export default theme;
